<h1>Your History</h1>

<% if @history_boxes %>
  <% @history_boxes.each do |box| %>
    <ul>
      <li><%= link_to box.title, box_path(box) %> | <%= box.month_year.strftime('%B %Y') %></li>
    </ul>
  <% end %>
<% else %>
 <p>You have no history.</p>
<% end %>
